{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "node app.js && cd app && npm install && npm run build && mv build .."
  },
  "author": "Gabriel Nunes",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "lodash": "^4.17.19",
    "path": "^0.12.7",
    "socket.io": "^2.3.0"
  }
}
